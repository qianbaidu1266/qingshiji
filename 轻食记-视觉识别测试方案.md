# 轻食记 · 视觉识别准确率测试方案

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-29 |
| 测试目标 | 验证视觉模型对中餐的识别准确率，决定拍照识别方案是否成立 |
| 测试方式 | Node.js 脚本 + 本地图片，不依赖小程序工程 |
| 预计耗时 | 采集 1 天 + 执行 0.5 天 + 分析 0.5 天 |
| 预计成本 | < 5 元 |

---

## 1. 为什么要先做这个测试

拍照识别在 PRD 中被定位为**核心快捷入口**（PRD 2.1.1）。若识别准确率不达标，会连锁影响：

- 用户每次都要大幅修改 → 快捷入口失去意义
- 热量数字偏差过大 → 触发 PRD 5.2 关注的信任问题
- 需要重新设计入口形态（改为"拍照 + 手动选菜品"）

因此这是一个**"测不出来就别往下走"**的技术前提。用不到 5 块钱和 2 天时间，规避两周开发返工的风险。

### 一个重要的认知：测试的主要价值是横向对比

严格意义上，食物热量的"真实值"本身就难以确定（烹饪用油量、食材部位、份量均存在波动）。因此本测试的**首要目标是横向对比 DeepSeek 与智谱的相对表现**，其次才是评估绝对准确率。

**决策依赖的是"哪家更准"，而不是"准确率是不是 85%"。**

### 为什么不能用"调研"替代实测

常见的三种替代思路，都无法支撑决策：

| 替代方式 | 为什么不适用 |
|---|---|
| **查公开评测榜单**（MMBench、MMMU 等） | 测的是通用视觉理解能力，以英文场景、西餐为主。中餐外卖、食堂餐盘、汤面几乎无覆盖，通用榜单排名与垂直场景表现相关性很弱 |
| **看厂商宣传的准确率** | 各家测试集不公开、不统一，数字不可比，也不可复现 |
| **直接问模型"你识别食物准不准"** | 模型不具备自我评估能力，且提问对象与被测对象可能是同一个 |

**更根本的原因：这个任务的难点不在"识别"，在"估算"。**

认出"这是一盘番茄炒蛋"，当前主流多模态模型都不难。真正困难、也真正决定产品成败的是后面几步：

| 难点 | 为什么估不准 | 影响 |
|---|---|---|
| **份量判断** | 一碗米饭是 150g 还是 250g？图片看不出碗的深度 | 等比影响热量 |
| **用油量** | 看不出炒这道菜放了多少油 | 中餐热量的 30%–40% 来自油脂，最大误差来源 |
| **汤汁算不算** | 牛肉面的汤喝不喝？各家策略不同 | 可造成 100–200 kcal 偏差 |
| **混装菜品分割** | 食堂餐盘里三样菜各占多少 | 无法拆分就无法估算 |

这些难点**没有任何公开 benchmark 覆盖**，因为答案依赖具体场景约定（例如"汤算不算"是你自己定的产品规则）。只能自己测。

> **类比**：这相当于想知道哪个翻译引擎翻译你们行业的文档更准。查通用翻译评测没用（测的是新闻小说），问引擎自己也没用，只能拿你们自己的文档实际翻一遍对比。

### 这个测试实际在回答三个问题

调研一个都回答不了，而这三个都是开工前必须定的：

1. **选哪家供应商**——不是谁"更强"，是谁在中餐热量估算上误差更小
2. **拍照识别这个形态成不成立**——决定首页主入口怎么设计
3. **UI 要不要强制显示"估算"标识**——取决于"高置信度答错"的比例

### 如果资源紧张：最小可行版本

若无法投入完整两天，可压缩为**只测 15 张 A 组图**（全部选连锁餐饮，标准答案最好找），约 2 小时完成。仍可回答最关键的两个问题：

- 两家供应商谁更准
- 热量误差的大致量级是否可接受

15 张不足以支撑精细结论，但足以支撑"拍照识别要不要继续做"这个方向性判断。

---

## 2. 测试集设计

### 2.1 分组策略

测试集分为两组，用途不同：

| 组 | 数量 | 特征 | 用途 |
|---|---|---|---|
| **A 组 · 可验证组** | 30 张 | 有可靠标准答案（连锁餐饮公开营养信息、包装食品营养标签、标准份量） | 计算量化准确率 |
| **B 组 · 真实场景组** | 20 张 | 真实外卖、食堂餐盘、困难场景，无精确标准答案 | 观察失败模式与鲁棒性 |

> **不要随机拍 50 张图。** 没有标准答案的图片无法计算误差，测试就退化成主观感受。A 组是量化依据，B 组是风险探查，两者缺一不可。

### 2.2 A 组构成（30 张，有标准答案）

| 子类 | 数量 | 示例 | 标准答案来源 |
|---|---|---|---|
| 连锁餐饮（公开营养信息） | 12 | 麦当劳巨无霸、肯德基吮指原味鸡、瑞幸拿铁、赛百味 6 寸三明治 | 品牌官网 / 门店公示的营养成分表 |
| 包装食品（有营养标签） | 8 | 便利店饭团、盒装牛奶、酸奶、坚果包、方便面 | 包装背面营养成分表 |
| 标准份量家常菜 | 10 | 番茄炒蛋、红烧肉、清炒时蔬、白米饭 | 按标准食谱 + 食材营养值计算 |

**采集要点**：连锁餐饮与包装食品的照片**必须拍到包装或能够明确辨识品牌与规格**，否则标准答案无法对应。

### 2.3 B 组构成（20 张，真实场景）

| 场景 | 数量 | 预期难点 |
|---|---|---|
| 外卖（含餐盒） | 6 | 餐盒遮挡、油汤混合、份量判断 |
| 食堂餐盘 | 5 | 多菜品混装、份量难以分割 |
| 汤面 / 汤粉 | 3 | 汤汁热量是否计入（关键难点） |
| 火锅 / 麻辣烫 / 拼盘 | 3 | 极端困难，预期高误差 |
| 拍摄条件不佳 | 3 | 光线暗、俯拍角度偏、部分遮挡 |

> B 组中的火锅与拼盘是**故意设置的困难样本**。目的不是期待模型答对，而是观察失败时的行为——是给出高置信度的错误答案（危险），还是承认不确定（可接受）。

### 2.4 图片规格

每张图准备两个版本，用于验证压缩影响：

| 版本 | 规格 | 用途 |
|---|---|---|
| 原图 | 手机直出（通常 3000×4000） | 基线对照 |
| 压缩图 | 长边 ≤ 1080px，质量 0.8 | 模拟小程序实际上传（PRD 技术方案 3.7） |

---

## 3. 标准答案（Ground Truth）准备

### 3.1 数据格式

为每张图标注，存为 `dataset/labels.json`：

```json
{
  "images": [
    {
      "id": "A01",
      "file": "A01_mcd_bigmac.jpg",
      "group": "A",
      "category": "连锁餐饮",
      "name": "麦当劳 巨无霸",
      "source": "麦当劳官网营养信息",
      "truth": {
        "totalWeight": 215,
        "calorie": 550,
        "protein": 25,
        "fat": 30,
        "carb": 46
      },
      "notes": "含面包、牛肉饼、酱料、蔬菜"
    },
    {
      "id": "B07",
      "file": "B07_noodle_soup.jpg",
      "group": "B",
      "category": "汤面",
      "name": "牛肉面（含汤）",
      "source": "估算",
      "truth": null,
      "notes": "重点关注：汤汁热量是否计入"
    }
  ]
}
```

### 3.2 A 组标准答案获取优先级

```
1. 品牌官方公示营养信息      → 最可靠，优先使用
2. 包装营养成分表            → 可靠，注意核对"每份"与"每100g"
3. 标准食谱计算              → 需记录计算过程，便于复核
```

**注意事项**：

- 包装食品注意区分"每 100g"与"每份"，标注时必须换算成实际份量
- 连锁餐饮不同地区的配方可能存在差异，标注时注明数据来源与日期
- 家常菜按标准食谱计算时，需明确记录的用油量（这是最大的误差来源）

### 3.3 B 组标注

B 组 `truth` 字段设为 `null`，但必须填写 `notes`，记录**预期难点**。分析时重点看模型在这些样本上的失败模式。

---

## 4. 评估指标

### 4.1 量化指标（基于 A 组）

| 指标 | 定义 | 计算方式 |
|---|---|---|
| **菜名识别准确率** | 识别出的菜品名称与标注一致或语义等价 | 人工判定，语义等价（如"番茄炒蛋"与"西红柿炒鸡蛋"）计为正确 |
| **热量 MAPE** | 平均绝对百分比误差 | `mean(|预测值 - 真实值| / 真实值) × 100%` |
| **热量命中率** | 误差 ≤ 20% 的样本占比 | 核心指标 |
| **严重偏差率** | 误差 > 40% 的样本占比 | 超过此比例的样本，用户必须大幅修改 |
| **营养素一致性** | 三大营养素折算热量与总热量的偏差 | 检验模型内部数值是否自洽 |

### 4.2 观察指标（基于 B 组与全过程）

| 指标 | 说明 |
|---|---|
| 响应时延 P50 / P95 | 直接影响用户等待体验 |
| JSON 格式失败率 | 返回无法解析的比例 |
| 图片压缩影响 | 压缩图 vs 原图的准确率差异 |
| 困难样本的失败模式 | 是"高置信度地答错"还是"承认不确定" |
| 单图 token 消耗 | 用于校验成本模型 |

### 4.3 判定标准

| 指标 | 通过 | 可接受（需调整设计） | 不通过 |
|---|---|---|---|
| 菜名识别准确率 | ≥ 80% | 65% – 80% | < 65% |
| 热量 MAPE | ≤ 20% | 20% – 35% | > 35% |
| 热量命中率（误差 ≤20%） | ≥ 65% | 45% – 65% | < 45% |
| 严重偏差率（误差 >40%） | ≤ 15% | 15% – 30% | > 30% |
| 响应时延 P95 | ≤ 8s | 8 – 15s | > 15s |
| JSON 失败率 | ≤ 2% | 2% – 8% | > 8% |

---

## 5. 测试执行

### 5.1 目录结构

```
vision-test/
├── dataset/
│   ├── images/           # 50 张原图 + 50 张压缩图
│   └── labels.json       # 标注数据
├── src/
│   ├── providers/
│   │   ├── deepseek.js   # DeepSeek 适配器
│   │   └── zhipu.js      # 智谱适配器
│   ├── prompt.js         # 统一 Prompt（与生产环境一致）
│   ├── evaluate.js       # 指标计算
│   └── index.js          # 主流程
├── results/
│   ├── raw/              # 原始返回，便于复核
│   └── report.md         # 测试报告
├── .env                  # API Key（加入 .gitignore）
└── package.json
```

### 5.2 Provider 适配器

关键设计：**两家共用同一套接口**，返回值归一化为统一结构，便于对比。

```javascript
// src/providers/deepseek.js
const axios = require('axios')

const MODEL = 'deepseek-v4-flash-vision-exp'

async function recognize({ imageUrl, prompt, systemPrompt }) {
  const started = Date.now()

  const res = await axios.post(
    'https://api.deepseek.com/chat/completions',
    {
      model: MODEL,
      messages: [
        { role: 'system', content: systemPrompt },
        {
          role: 'user',
          content: [
            { type: 'text', text: prompt },
            { type: 'image_url', image_url: { url: imageUrl } }
          ]
        }
      ],
      response_format: { type: 'json_object' },
      temperature: 0.3,
      max_tokens: 1200
    },
    {
      headers: { Authorization: `Bearer ${process.env.DEEPSEEK_API_KEY}` },
      timeout: 45000
    }
  )

  return {
    latency: Date.now() - started,
    usage: res.data.usage,
    raw: res.data.choices[0].message.content
  }
}

module.exports = { recognize, MODEL }
```

```javascript
// src/providers/zhipu.js
const axios = require('axios')

const MODEL = process.env.ZHIPU_VISION_MODEL || 'glm-4.6v'

async function recognize({ imageUrl, prompt, systemPrompt }) {
  const started = Date.now()

  const res = await axios.post(
    'https://open.bigmodel.cn/api/paas/v4/chat/completions',
    {
      model: MODEL,
      messages: [
        { role: 'system', content: systemPrompt },
        {
          role: 'user',
          content: [
            { type: 'text', text: prompt },
            { type: 'image_url', image_url: { url: imageUrl } }
          ]
        }
      ],
      temperature: 0.3,
      max_tokens: 1200
    },
    {
      headers: { Authorization: `Bearer ${process.env.ZHIPU_API_KEY}` },
      timeout: 45000
    }
  )

  return {
    latency: Date.now() - started,
    usage: res.data.usage,
    raw: res.data.choices[0].message.content
  }
}

module.exports = { recognize, MODEL }
```

> 智谱的视觉模型 ID 需在控制台确认当前可用名称（`glm-4.6v` / `glm-4.5v` 等），并在 `.env` 中通过 `ZHIPU_VISION_MODEL` 配置，**不要硬编码**。

### 5.3 Prompt（与生产环境保持一致）

测试必须用**与生产完全相同的 Prompt**，否则结论不可迁移。

```javascript
// src/prompt.js
const SYSTEM_PROMPT = `你是营养分析助手。识别图片中的食物，输出 JSON 格式结果。

【背景】
用户为中国年轻上班族，常见就餐场景为外卖、食堂、便利店。
份量请按中国大陆常见份量估算。

【要求】
1. 只输出 JSON，不要任何解释性文字
2. 每种食物给出：名称、份量(g)、热量(kcal)、蛋白质(g)、脂肪(g)、碳水(g)、置信度(0-1)
3. 份量取常见值，不要精确到个位
4. 外卖份量通常比家常份量大 15% 左右，按此修正
5. 汤汁、酱料热量需单独估算并计入
6. 无法确定的食物也要给出估算，置信度标为 0.6 以下

【输出格式】
{
  "items": [
    {"name": "番茄炒蛋", "portion": 200, "calorie": 180,
     "protein": 12, "fat": 11, "carb": 6, "confidence": 0.85}
  ],
  "total": {"calorie": 180, "protein": 12, "fat": 11, "carb": 6},
  "note": "一句话提示"
}`

const USER_PROMPT = '识别这张图片中的全部食物，按上述 JSON 格式输出。'

module.exports = { SYSTEM_PROMPT, USER_PROMPT }
```

### 5.4 主流程

```javascript
// src/index.js
const fs = require('fs')
const path = require('path')
const deepseek = require('./providers/deepseek')
const zhipu = require('./providers/zhipu')
const { SYSTEM_PROMPT, USER_PROMPT } = require('./prompt')

const PROVIDERS = { deepseek, zhipu }

function toDataUrl(file) {
  const buf = fs.readFileSync(file)
  const ext = path.extname(file).slice(1)
  const mime = ext === 'jpg' ? 'jpeg' : ext
  return `data:image/${mime};base64,${buf.toString('base64')}`
}

async function run() {
  const labels = JSON.parse(
    fs.readFileSync('./dataset/labels.json', 'utf8')
  )
  const results = []

  for (const item of labels.images) {
    for (const [name, provider] of Object.entries(PROVIDERS)) {
      for (const variant of ['origin', 'compressed']) {
        const file = path.join(
          './dataset/images',
          variant,
          item.file
        )
        if (!fs.existsSync(file)) continue

        const record = {
          id: item.id, group: item.group, provider: name, variant,
          category: item.category, truth: item.truth
        }

        try {
          const res = await provider.recognize({
            imageUrl: toDataUrl(file),
            prompt: USER_PROMPT,
            systemPrompt: SYSTEM_PROMPT
          })
          record.latency = res.latency
          record.usage = res.usage
          record.raw = res.raw

          try {
            record.parsed = JSON.parse(res.raw)
            record.parseOk = true
          } catch {
            record.parseOk = false
          }
        } catch (err) {
          record.error = err.message
        }

        results.push(record)
        console.log(`[${name}/${variant}] ${item.id} done`)

        // 控制请求频率，避免触发限流
        await new Promise(r => setTimeout(r, 1200))
      }
    }
  }

  fs.writeFileSync(
    './results/raw/results.json',
    JSON.stringify(results, null, 2)
  )
  console.log('完成，结果已写入 results/raw/results.json')
}

run()
```

### 5.5 指标计算

```javascript
// src/evaluate.js
function evaluate(results) {
  const rows = results.filter(
    r => r.group === 'A' && r.truth && r.parseOk
  )

  const stats = {}
  for (const provider of ['deepseek', 'zhipu']) {
    const subset = rows.filter(
      r => r.provider === provider && r.variant === 'origin'
    )

    const errors = subset.map(r => {
      const pred = r.parsed.total.calorie
      const truth = r.truth.calorie
      return Math.abs(pred - truth) / truth
    })

    const latencies = subset.map(r => r.latency).sort((a, b) => a - b)

    stats[provider] = {
      samples: subset.length,
      mape: mean(errors) * 100,
      hitRate: errors.filter(e => e <= 0.2).length / errors.length * 100,
      severeRate: errors.filter(e => e > 0.4).length / errors.length * 100,
      latencyP50: percentile(latencies, 0.5),
      latencyP95: percentile(latencies, 0.95),
      parseFailRate:
        results.filter(r => r.provider === provider && !r.parseOk).length
        / results.filter(r => r.provider === provider).length * 100
    }
  }
  return stats
}

const mean = a => a.reduce((s, v) => s + v, 0) / a.length
const percentile = (sorted, p) =>
  sorted[Math.floor(sorted.length * p)]

module.exports = { evaluate }
```

### 5.6 执行

```bash
npm init -y
npm i axios dotenv
# 配置 .env：DEEPSEEK_API_KEY / ZHIPU_API_KEY / ZHIPU_VISION_MODEL
node src/index.js        # 采集，约 15-25 分钟
node src/evaluate.js     # 计算指标
```

**执行注意**：

- 请求间隔设为 1.2 秒，避免触发限流
- 全部原始返回写入 `results/raw/`，便于事后复核
- 若某张图返回异常，保留错误信息而非中断整个流程

---

## 6. 结果分析与决策

### 6.1 决策树

```
热量 MAPE
├─ ≤ 20%  → ✅ 拍照识别可作为主入口，按 PRD 2.1.1 实施
│
├─ 20%–35% → ⚠️ 可用，但需调整产品设计
│            - 识别结果默认展开为可编辑状态，而非展示态
│            - 强化「估算」标识（PRD 3.6）
│            - 首页优先级下调，手动入口同等权重
│            - 考虑增加「确认份量」步骤
│
└─ > 35%   → ❌ 重新设计入口形态
             - 方案一：改为「拍照 + 从候选列表选择菜品」
               （模型识别候选，人工确认，兼顾效率与准确）
             - 方案二：拍照识别降为辅助，主入口改为手动搜索 + 食物库
             - 方案三：缩小识别范围，仅支持食物库内已有的常见菜品
```

### 6.2 供应商选择

| 情况 | 决策 |
|---|---|
| 准确率差异 < 5 个百分点 | **选便宜且不分峰谷的**（大概率是智谱），省去错峰架构 |
| 准确率差异 5–15 个百分点 | 按「准确率权重 × 成本」综合判断，倾向准确率 |
| 准确率差异 > 15 个百分点 | 选准确率高的，成本问题通过缓存与频次控制解决 |

### 6.3 压缩影响判定

| 压缩后准确率下降 | 决策 |
|---|---|
| < 3 个百分点 | 采用 1080px 压缩（省存储与传输） |
| 3–8 个百分点 | 提高至 1440px，评估成本增量 |
| > 8 个百分点 | 重新评估压缩策略，可能需传原图 |

### 6.4 B 组失败模式分析（定性）

重点关注三类行为：

| 失败模式 | 风险等级 | 应对 |
|---|---|---|
| **高置信度地答错**（confidence > 0.8 但误差 > 40%） | 🔴 高危 | 用户会信任错误数字，需强制展示"估算"标识 |
| 低置信度地答错（confidence < 0.6） | 🟡 可接受 | 已有提示机制 |
| 拒绝识别 / 返回空 | 🟢 良好 | 直接引导手动输入 |

**"高置信度地答错"是最危险的失败模式**——它会让用户相信一个错误的数字，直接损害产品信任基础。若此类样本占比超过 15%，无论 MAPE 多少，都必须强制在 UI 上展示不确定性提示。

---

## 7. 小程序特有验证项

脚本测试完成后，需在真机补充验证以下项（约半天）：

| 验证项 | 说明 | 判定 |
|---|---|---|
| 真机拍摄质量 | 餐厅实拍（光线复杂、俯拍角度）与测试集的差异 | 补充拍摄 10 张真实就餐图复测 |
| 云函数端到端时延 | 含图片上传 + 云函数调用 + 模型响应的总耗时 | ≤ 12s 可接受 |
| 弱网表现 | 4G / 弱 WiFi 下的成功率与超时率 | 失败时须有手动兜底 |
| 图片压缩实机效果 | 小程序端压缩后的实际识别表现 | 与脚本结论一致 |
| 连续拍摄稳定性 | 连续识别 5 次的成功率 | ≥ 95% |

---

## 8. 排期与交付物

| 阶段 | 任务 | 耗时 |
|---|---|---|
| D1 | 采集测试集（A 组 30 张 + B 组 20 张），标注 ground truth | 1 天 |
| D2 上午 | 编写并执行测试脚本 | 0.5 天 |
| D2 下午 | 计算指标、生成报告、做出决策 | 0.5 天 |
| D3 | 真机补充验证（可选，视脚本结论而定） | 0.5 天 |

**交付物**：

1. `results/raw/results.json` —— 全部原始返回
2. `results/report.md` —— 测试报告，含以下结论：
   - 两家供应商的量化对比表
   - 各类别（外卖 / 食堂 / 包装 / 汤面）的分项准确率
   - 响应时延与成本实测值
   - **明确结论**：采用哪家供应商、拍照识别采用何种形态
3. 更新后的技术方案文档（补充实测数据，替换估算值）

---

## 附录：测试集采集清单模板

| ID | 分组 | 类别 | 内容 | 标准答案来源 | 已采集 | 已标注 |
|---|---|---|---|---|---|---|
| A01 | A | 连锁餐饮 | 麦当劳 巨无霸 | 官网营养信息 | ☐ | ☐ |
| A02 | A | 连锁餐饮 | 肯德基 吮指原味鸡 | 官网营养信息 | ☐ | ☐ |
| A03 | A | 连锁餐饮 | 瑞幸 拿铁（大杯） | 官网营养信息 | ☐ | ☐ |
| A04 | A | 连锁餐饮 | 赛百味 6寸火腿三明治 | 官网营养信息 | ☐ | ☐ |
| A05 | A | 连锁餐饮 | 星巴克 可颂 | 官网营养信息 | ☐ | ☐ |
| A06 | A | 连锁餐饮 | 汉堡王 皇堡 | 官网营养信息 | ☐ | ☐ |
| A07 | A | 连锁餐饮 | 必胜客 披萨（1片） | 官网营养信息 | ☐ | ☐ |
| A08 | A | 连锁餐饮 | 真功夫 香菇鸡肉饭 | 门店公示 | ☐ | ☐ |
| A09 | A | 连锁餐饮 | 老乡鸡 肥西老母鸡汤 | 门店公示 | ☐ | ☐ |
| A10 | A | 连锁餐饮 | 和府捞面 草本猪软骨面 | 门店公示 | ☐ | ☐ |
| A11 | A | 连锁餐饮 | 米村拌饭 石锅拌饭 | 门店公示 | ☐ | ☐ |
| A12 | A | 连锁餐饮 | 喜茶 多肉葡萄 | 官网营养信息 | ☐ | ☐ |
| A13 | A | 包装食品 | 便利店 饭团 | 包装营养标签 | ☐ | ☐ |
| A14 | A | 包装食品 | 盒装牛奶 250ml | 包装营养标签 | ☐ | ☐ |
| A15 | A | 包装食品 | 无糖酸奶 | 包装营养标签 | ☐ | ☐ |
| A16 | A | 包装食品 | 每日坚果 | 包装营养标签 | ☐ | ☐ |
| A17 | A | 包装食品 | 桶装方便面 | 包装营养标签 | ☐ | ☐ |
| A18 | A | 包装食品 | 全麦面包 | 包装营养标签 | ☐ | ☐ |
| A19 | A | 包装食品 | 鸡胸肉即食包 | 包装营养标签 | ☐ | ☐ |
| A20 | A | 包装食品 | 蛋白棒 | 包装营养标签 | ☐ | ☐ |
| A21 | A | 家常菜 | 番茄炒蛋（一份） | 标准食谱计算 | ☐ | ☐ |
| A22 | A | 家常菜 | 红烧肉（一份） | 标准食谱计算 | ☐ | ☐ |
| A23 | A | 家常菜 | 清炒时蔬 | 标准食谱计算 | ☐ | ☐ |
| A24 | A | 家常菜 | 白米饭（一碗 150g） | 标准值 | ☐ | ☐ |
| A25 | A | 家常菜 | 水煮蛋 ×2 | 标准值 | ☐ | ☐ |
| A26 | A | 家常菜 | 香煎鸡胸（120g） | 标准食谱计算 | ☐ | ☐ |
| A27 | A | 家常菜 | 紫菜蛋花汤 | 标准食谱计算 | ☐ | ☐ |
| A28 | A | 家常菜 | 清蒸鲈鱼 | 标准食谱计算 | ☐ | ☐ |
| A29 | A | 家常菜 | 麻婆豆腐 | 标准食谱计算 | ☐ | ☐ |
| A30 | A | 家常菜 | 杂粮饭（一碗） | 标准食谱计算 | ☐ | ☐ |
| B01–B06 | B | 外卖 | 真实外卖订单（含餐盒） | — | ☐ | ☐ |
| B07–B09 | B | 汤面 | 牛肉面 / 米线 / 馄饨 | — | ☐ | ☐ |
| B10–B14 | B | 食堂 | 食堂餐盘（多菜品混装） | — | ☐ | ☐ |
| B15–B17 | B | 困难 | 火锅 / 麻辣烫 / 多人拼盘 | — | ☐ | ☐ |
| B18–B20 | B | 拍摄不佳 | 光线暗 / 角度偏 / 遮挡 | — | ☐ | ☐ |

> A 组的标准答案来源优先级：品牌官方 > 包装标签 > 食谱计算。标注时须记录来源与计算过程，便于复核。
