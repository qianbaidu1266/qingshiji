# 轻食记 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | 轻食记 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-29 |
| 产品形态 | 微信小程序（原生 + TypeScript） |
| 目标平台 | 微信 |
| 主体类型 | 个体工商户（需开通微信支付） |

---

## 1. 产品概述

### 1.1 一句话定义

面向想科学减脂的青年人群的饮食管理工具：记录每天吃了什么，分析热量并给出改进建议；不知道吃什么时提供推荐；基于身高体重与目标生成可执行的饮食计划与运动计划。

### 1.2 目标用户

22–35 岁青年人群，主要为一二线城市上班族，有明确减脂意愿，曾尝试过节食、代餐、健身卡等方式但未能坚持。

| 特征 | 具体表现 | 对产品的要求 |
|---|---|---|
| 时间碎片化 | 无法投入时间系统学习营养知识 | 零学习成本，打开即用 |
| 多次失败经历 | 节食、代餐、健身卡均半途而废 | 核心是可持续性，不是知识量 |
| 决策疲劳 | 工作已消耗大量意志力 | 替用户做决定，减少选择 |
| 缺乏专业知识 | 知道要"少吃多动"，不知道具体怎么做 | 给具体行动指令，不给抽象概念 |
| 真实就餐场景 | 外卖、食堂、便利店为主，极少自炊 | 推荐必须匹配非自炊场景 |

### 1.3 核心价值分层

| 层级 | 解决的用户问题 | 功能模块 | 定位 |
|---|---|---|---|
| L1 执行层 | 记录太麻烦，坚持不下来 | 拍照识别 + 手动记录 | 入场券，非壁垒 |
| L2 决策层 | 不知道下一顿吃什么 | 饮食推荐 | 留存关键 |
| L3 方案层 | 怕方法不对，白费力气 | 减脂方案（饮食计划 + 运动计划） | 核心卖点 + 付费点 |
| L4 陪伴层 | 没人监督就放弃 | AI 改进建议 + 趋势复盘 | 长期留存 |

> **产品命题**：不用动脑的减脂。每一层设计都在替用户做决定、降低阻力。

---

## 2. 功能需求

### 2.1 模块一：记录

#### 2.1.1 拍照识别（快捷输入）

| 项 | 说明 |
|---|---|
| 触发 | 首页主按钮「拍照识别」 |
| 流程 | 拍照 / 相册选取 → 上传 → 云端视觉模型识别 → 返回食物列表与热量 → 用户确认或编辑 → 写入记录 |
| 输出字段 | 食物名称、估算份量（g/ml）、热量、蛋白质、脂肪、碳水、置信度 |
| 容错设计 | **识别结果必须可编辑**。识别准确率目标 ≥ 80%，不追求更高，因为用户可修正 |
| 降级 | 识别失败时提供手动搜索入口，不允许流程中断 |
| 免费策略 | 免费且不限次（记录频率直接决定留存与数据质量，不在此设卡） |

#### 2.1.2 手动添加

- 支持关键词搜索本地食物库
- 支持自定义份量（克数 / 常见份量单位：碗、个、份、杯）
- 支持完全自定义食物（用户手填名称与热量，标记为自定义）
- 支持「历史记录」快速复选（近期吃过的、常吃的）

#### 2.1.3 记录管理

- 餐次分类：早餐 / 午餐 / 晚餐 / 加餐
- 每条记录支持编辑（名称、份量、热量）与删除
- 支持补记历史日期
- 单条记录展示来源标识：拍照识别 / 手动 / 估算（估算数据需明示不确定性）

### 2.2 模块二：分析

#### 2.2.1 每日汇总

- 总摄入热量 vs 目标热量（环形进度）
- 三大营养素实际值 vs 目标值（蛋白质 / 脂肪 / 碳水）
- 按餐次分组的热量分布

#### 2.2.2 AI 改进建议

| 项 | 说明 |
|---|---|
| 触发时机 | 当日记录 ≥ 1 条时，首页展示；每日首次打开时生成并缓存 |
| 输入 | 当日饮食明细 + 目标热量与营养目标 + 用户档案 |
| 输出 | 一段自然语言点评（3–5 句）+ 1–3 条具体可执行建议 |
| 内容原则 | 只给行动指令，不给概念解释 |
| 生成方式 | 云函数调用 LLM，API Key 不落前端 |

**示例输出格式**：

```
今日摄入 1842 大卡，超出目标 362。
- 蛋白质还差 31g，晚餐加两个水煮蛋或一份鸡胸即可补足
- 下午 3 点那杯奶茶占了 420 大卡，占全天 23%，建议替换为无糖茶
- 蔬菜摄入偏少，明天午餐加一份绿叶菜
```

### 2.3 模块三：推荐

#### 2.3.1 下一顿吃什么

| 项 | 说明 |
|---|---|
| 触发 | 首页推荐位常驻，点击展开；临近餐点时间推送订阅消息 |
| 输入 | 当前时间、今日已摄入、剩余热量额度、用户口味偏好、就餐场景 |
| 输出 | 3 个具体选项，每项含：食物组合、估算热量、推荐理由 |
| 场景适配 | 支持外卖 / 食堂 / 便利店 / 自炊四种场景切换 |

**示例输出**：

```
午餐 · 剩余额度 620 大卡

1. 潮汕牛肉粿条（少汤）      约 540 大卡
   蛋白质充足，避开红烧类即可

2. 轻食鸡胸藜麦碗            约 480 大卡
   你今天蛋白质还差不少，这个最补齐

3. 沙县小吃：蒸饺 + 拌青菜    约 560 大卡
   注意蘸料只取一半
```

#### 2.3.2 场景化推荐

针对上班族真实就餐场景提供专项建议：

- **外卖**：具体菜品点评（能吃什么、怎么点、酱料/份量调整建议）
- **食堂**：窗口组合建议（选哪两个窗口、避开哪些烹饪方式）
- **便利店**：加班场景的应急选择清单

> 市面 90% 健康饮食产品默认用户自炊，与上班族真实场景存在严重错位，这是本模块的主要机会点。

### 2.4 模块四：方案（核心卖点）

#### 2.4.1 目标设置引导

| 步骤 | 采集字段 | 校验规则 |
|---|---|---|
| 1 基础档案 | 性别、出生年份、身高、当前体重 | 常规范围校验 |
| 2 活动水平 | 五档活动系数选择 | 单选 |
| 3 目标设定 | 目标体重、期望达成周期 | **触发极端目标拦截（见 3.3）** |
| 4 偏好采集 | 忌口、饮食偏好、可运动时段、运动基础 | 多选，可跳过 |
| 5 生成方案 | 展示计算结果 + 饮食计划 + 运动计划 | — |

#### 2.4.2 饮食计划

- 输出周期：周维度（7 天），可按日查看
- 每餐给出：食物组合建议、目标热量区间、执行要点
- 基于目标热量与营养素配比生成（数值由代码计算，LLM 负责组合与表述）

#### 2.4.3 运动计划

| 项 | 说明 |
|---|---|
| 输出周期 | 周维度（7 天） |
| 内容 | 每周几、运动类型、时长、强度、执行要点 |
| 消耗估算 | **给范围值，不给精确值**（如「约 200–250 大卡」） |
| 生成方式 | LLM 生成，基于用户运动基础、可运动时段、目标缺口 |
| 边界 | **不建 MET 运动数据库**，不做精确消耗计算 |

> 运动计划与运动消耗数据库是两个问题。生成一份合理的周计划是 LLM 的强项，成本极低；而 MET 数据库需要每种运动 × 体重 × 时长建模，复杂度远高于收益。V1 只做前者。

#### 2.4.4 方案调整

- 支持按周重新生成
- 支持调整偏好（忌口、运动时段、强度）后重新生成
- 每周基于真实执行数据（体重、记录完成度）自动提示是否调整

### 2.5 模块五：趋势

#### 2.5.1 体重曲线

| 项 | 说明 |
|---|---|
| 主曲线 | **7 日移动平均线**（需 ≥ 7 天数据后开始绘制） |
| 辅助 | 每日实测点以淡化样式作为背景 |
| 记录引导 | 建议固定时间记录（晨起空腹、如厕后），首次记录时说明 |
| 文案原则 | 强调趋势（「近 7 天平均下降 0.4kg」），不强调单点数值 |

> 人体日体重波动 1–2kg 属常态。直接连接日线会使用户因正常波动产生挫败感，是导致放弃的首要原因。

#### 2.5.2 其他趋势

- 热量摄入趋势（柱状，含目标线）
- 三大营养素达标率
- 打卡日历（记录完成度）
- 周报（AI 生成，含本周总结 + 下周调整建议）

### 2.6 模块六：我的

- 个人档案维护（身高、体重、活动水平）
- 目标管理（修改目标、结束目标）
- 会员与付费
- 设置（提醒时间、隐私、清除数据）

### 2.7 模块七：社交与裂变

#### 2.7.1 设计前提

减脂这件事的社交属性有一个反直觉的特征：

> **用户不愿意公开"我在减肥"，但愿意公开"我瘦了"。**

公开"在努力"意味着承担失败的风险（没瘦下来会丢脸），公开"成果"则是纯粹的炫耀。因此：

| 可做 | 不可做 |
|---|---|
| 分享成果（连续记录天数、体重变化） | 分享打卡过程（"我今天又打卡了"） |
| 分享有趣内容（AI 毒舌点评） | 分享枯燥数据（热量明细表） |
| 分享具体知识（这份外卖 520 大卡） | 分享抽象状态（我在减脂第 12 天） |

**反面清单（明确不做）**：

- **开放社区 / 论坛**：减脂类产品无内容供给，冷启动必然失败，不要做
- **好友排行榜**：让落后者产生挫败感，反而加速流失
- **分享解锁功能**：属诱导分享，违规且严重伤害体验

#### 2.7.2 战绩卡片

| 项 | 说明 |
|---|---|
| 内容 | 连续记录天数、本周体重变化、AI 点评金句、小程序码 |
| 触发 | 周报生成后、达成里程碑时（连续 7 天 / 减重 1kg / 目标达成） |
| 关键设计 | **AI 点评是最佳分享素材** |

> 用户转发「AI 说我的蔬菜摄入只够喂饱一只仓鼠」是**自嘲**——自嘲是安全的社交货币。而转发「我在减肥第 12 天」是**宣告**——宣告失败会很丢脸，所以用户不会转。这是本模块能否跑通的关键。

#### 2.7.3 减脂搭子

| 项 | 设计 |
|---|---|
| 规模 | **2–5 人小群**，不做大群 |
| 交互 | 仅互相可见进度 + 一键鼓励（预设短语），**不做 IM 聊天** |
| 匹配 | 按目标减重量、起始体重、活跃时段匹配 |
| 退出 | 允许随时退出，不通知其他成员 |

**为什么不做 IM**：聊天是负担，而目标用户已经处于决策疲劳状态。轻量的"看到对方今天记录了"就够了，一旦要求对话，功能很快会变成死群。

**为什么是小群**：大群必然沉默，小群才有真实的陪伴压力。

#### 2.7.4 饮食内容沉淀（UGC 自动化）

用户每次拍照识别 + AI 点评，都会产生一条结构化内容：「某菜品、某份量、多少大卡、AI 建议」。这些内容可沉淀为可检索的内容库：

| 价值 | 说明 |
|---|---|
| UGC 成本趋近于零 | 内容由 AI 生成，用户只需确认，不需要主动创作 |
| 天然可分享 | 「这家店的麻辣烫 520 大卡，AI 说汤别喝」具备实用传播价值 |
| 搜索友好 | 覆盖「麻辣烫 热量」「减脂 外卖」等长尾搜索词，带来自然流量 |
| 数据资产 | 积累真实中餐饮食数据，构成长期壁垒 |

这是本产品少有的、能形成**网络效应**的设计：用户越多 → 内容越多 → 搜索流量越大 → 用户越多。

#### 2.7.5 订阅消息召回

严格来说不属于社交，但它是留存的第一手段，优先级高于所有社交功能。

| 场景 | 时机 | 内容 |
|---|---|---|
| 餐点提醒 | 早/午/晚预设时间 | 「该记录午餐了，今天还剩 620 大卡额度」 |
| 补记提醒 | 当日 21:00 未记录 | 「今天还没记录，补上只要 10 秒」 |
| 周报提醒 | 每周日 20:00 | 「本周报告已生成，平均下降 0.4kg」 |

#### 2.7.6 邀请机制

| 类型 | 处理 |
|---|---|
| 邀请好友结伴减脂 | **可做**，非利益诱导，属正常功能 |
| 分享解锁功能 / 分享得奖励 | **不可做**，属诱导分享，违规且伤害体验 |

---

## 3. 核心业务规则

### 3.1 基础代谢率（BMR）

采用 Mifflin-St Jeor 公式：

```
男性：BMR = 10 × 体重(kg) + 6.25 × 身高(cm) − 5 × 年龄 + 5
女性：BMR = 10 × 体重(kg) + 6.25 × 身高(cm) − 5 × 年龄 − 161
```

### 3.2 每日总消耗（TDEE）

```
TDEE = BMR × 活动系数
```

| 活动水平 | 系数 | 描述 |
|---|---|---|
| 久坐 | 1.20 | 几乎不运动，办公室工作 |
| 轻度 | 1.375 | 每周 1–3 次轻度运动 |
| 中度 | 1.55 | 每周 3–5 次中等强度运动 |
| 高度 | 1.725 | 每周 6–7 次高强度运动 |
| 极高 | 1.90 | 体力劳动或每日高强度训练 |

### 3.3 减脂目标热量与极端目标拦截

```
每日热量缺口 = 每周目标减重(kg) × 7700 / 7
目标热量     = TDEE − 每日热量缺口
```

**硬约束（不可绕过）**：

1. **目标热量 ≥ BMR**。低于基础代谢的方案一律拒绝生成。
2. **每周减重速率 ≤ 当前体重的 1%**。超出即判定为激进目标。
3. 触发拦截时，**必须给出替代方案与解释**，不允许仅弹出警告后放行。

**拦截示例**（数值经 `core/nutrition.ts` 实测验证）：

```
用户：女性，60kg，160cm，28 岁，久坐，目标 1 个月减 10 斤

计算结果：
  BMR  = 10×60 + 6.25×160 − 5×28 − 161 = 1299
  TDEE = 1299 × 1.2 = 1559
  目标速率 = 5kg / 4 周 = 1.25kg/周 = 2.08% 体重/周  → 超出 1% 上限
  所需缺口 = 1.25 × 7700 / 7 = 1375 kcal/天
  目标热量 = 1559 − 1375 = 184 kcal/天  → 远低于 BMR 1299，不可执行

系统响应：
  按你的目标，每天需要只摄入约 184 大卡，这远低于身体基础消耗
  （1299 大卡）。这种吃法会先掉肌肉、代谢下降，结束后几乎必然反弹。

  建议调整为：每周减 0.2kg，每天摄入 1339 大卡，预计 25 周达成。
  （蛋白 108g / 脂肪 54g / 碳水 105g）

  另外：你的基础代谢占日常消耗的比例很高，单纯控制饮食的空间有限。
  如果把活动水平提高到「每周运动 3–5 次」，同样的目标只需要 10 周，
  每天还能吃到 1463 大卡。

  [ 接受建议方案 ]  [ 我仍要自己设定 ]
```

> **重要产品洞察（写代码时实测发现）**
>
> 久坐人群的减脂空间远小于直觉预期。以上述 60kg 久坐女性为例：
> `TDEE − BMR = 1559 − 1299 = 260 kcal/天`，即每天最多只能有 260 大卡缺口，
> 对应**每周最多减 0.236kg**——即便「每周减 0.25kg」这种看似温和的目标，
> 对她也会触及 BMR 下限而被拦截。
>
> 这意味着：
> 1. **拦截会频繁触发**，不是边缘情况。文案质量直接决定用户是否流失，必须认真写，不能只弹一句「目标不合理」。
> 2. **增加运动是所有建议里性价比最高的一条**。上例中同样目标从 25 周缩短到 10 周，且每天能多吃 124 大卡——这是运动计划模块最重要的价值主张，应作为方案页的核心卖点文案。
> 3. 目标设置页应**主动展示活动水平对达成周期的影响**（切换活动系数时实时刷新预计周数），让用户在设置阶段就理解「多动比少吃更有效」。

### 3.4 营养素配比

计算顺序固定：先定蛋白质 → 再定脂肪 → 碳水补足剩余热量。

```
蛋白质目标 = 体重(kg) × 1.8 g        （减脂期区间 1.6–2.2）
脂肪目标   = 体重(kg) × 0.9 g        （区间 0.8–1.0）
碳水目标   = (目标热量 − 蛋白质×4 − 脂肪×9) / 4
```

### 3.5 体重移动平均

```
MA7(t) = ( W(t) + W(t−1) + ... + W(t−6) ) / 7
```

- 数据量 < 7 天时，不绘制主曲线，仅展示实测点与提示文案
- 缺失日期按前值填充（不因漏记中断曲线）

### 3.6 食物数据来源优先级

```
1. 本地食物库精确匹配      → 采用，标记 source = library，isEstimated = false
2. 用户自定义历史记录      → 采用，标记 source = manual
3. LLM 估算               → 采用，标记 source = ai_estimate，isEstimated = true
```

所有 `isEstimated = true` 的数据在 UI 上需有可辨识的弱提示，且允许一键修正。

---

## 4. 数据模型

数据库：微信云开发（文档型）。以下为集合设计。

### 4.1 users（用户档案）

| 字段 | 类型 | 说明 |
|---|---|---|
| _openid | string | 主键 |
| nickname | string | 昵称 |
| avatarUrl | string | 头像 |
| gender | enum | male / female |
| birthYear | number | 出生年份 |
| height | number | 身高（cm） |
| activityLevel | number | 活动系数 |
| createdAt | date | 注册时间 |

### 4.2 goals（目标）

| 字段 | 类型 | 说明 |
|---|---|---|
| _id | string | — |
| _openid | string | 归属用户 |
| startWeight | number | 起始体重（kg） |
| currentWeight | number | 当前体重（kg） |
| targetWeight | number | 目标体重（kg） |
| weeklyRate | number | 每周目标减重（kg） |
| bmr | number | 计算所得，冗余存储 |
| tdee | number | 计算所得 |
| calorieTarget | number | 每日目标热量 |
| proteinTarget | number | 蛋白质目标（g） |
| fatTarget | number | 脂肪目标（g） |
| carbTarget | number | 碳水目标（g） |
| status | enum | active / achieved / abandoned |
| createdAt | date | — |

### 4.3 foodLogs（饮食记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| _id | string | — |
| _openid | string | — |
| date | string | YYYY-MM-DD，用于日维度聚合 |
| mealType | enum | breakfast / lunch / dinner / snack |
| foodName | string | 食物名称 |
| portion | number | 份量（g 或 ml） |
| portionLabel | string | 份量描述（如「1 碗」「2 个」） |
| calorie | number | 热量（kcal） |
| protein | number | 蛋白质（g） |
| fat | number | 脂肪（g） |
| carb | number | 碳水（g） |
| source | enum | photo / manual / library / ai_estimate |
| isEstimated | boolean | 是否为估算值 |
| imageUrl | string | 拍照识别原图（可选） |
| loggedAt | date | — |

索引建议：`(_openid, date)` 复合索引，`(_openid, date, mealType)`

### 4.4 foodLibrary（食物库）

| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 标准名称 |
| aliases | array | 别名数组，用于搜索匹配 |
| category | string | 分类 |
| caloriePer100g | number | 每 100g 热量 |
| proteinPer100g | number | 每 100g 蛋白质 |
| fatPer100g | number | 每 100g 脂肪 |
| carbPer100g | number | 每 100g 碳水 |
| commonPortions | array | 常见份量 `[{label, grams}]` |

初期规模目标：覆盖常见中餐与外卖菜品 300–500 条，覆盖 80% 记录场景。

### 4.5 weightLogs（体重记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| _openid | string | — |
| date | string | YYYY-MM-DD，唯一 |
| weight | number | kg，保留 1 位小数 |
| note | string | 备注（可选） |

### 4.6 plans（方案）

| 字段 | 类型 | 说明 |
|---|---|---|
| _openid | string | — |
| type | enum | diet / exercise |
| period | enum | daily / weekly |
| startDate | string | 方案起始日 |
| content | object | 结构化方案内容 |
| generatedAt | date | 生成时间 |
| version | number | 版本号，用于迭代追踪 |

### 4.7 dailyAnalysis（每日分析缓存）

| 字段 | 类型 | 说明 |
|---|---|---|
| _openid | string | — |
| date | string | YYYY-MM-DD |
| totalCalorie | number | — |
| totalProtein | number | — |
| totalFat | number | — |
| totalCarb | number | — |
| advice | string | AI 生成的改进建议 |
| generatedAt | date | 用于判断是否需要重新生成 |

---

## 5. 技术架构

### 5.1 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 微信原生小程序 + TypeScript | 包体积最小，Canvas / 相机 / 分享等原生能力调用最直接 |
| 后端 | 微信云开发（云函数 + 云数据库 + 云存储） | 省服务器运维、免备案，适合小团队快速上线 |
| 视觉识别 | 云端视觉大模型 API | 经云函数中转 |
| 文本生成 | LLM API | 经云函数中转，Key 不落前端 |
| 图表 | ECharts for 小程序 或自绘 Canvas | 体重曲线、热量趋势 |

### 5.2 架构原则：计算层与表达层严格分离

这是本项目最重要的工程原则。

| 层 | 职责 | 实现 | 特性 |
|---|---|---|---|
| **计算层** | BMR、TDEE、目标热量、热量缺口、营养素配比、移动平均 | **纯 TypeScript 函数，前端本地执行** | 确定性，同样输入必然同样输出，无网络往返 |
| **表达层** | 把算好的数字翻译为饮食计划、运动计划、改进建议 | 云函数 → LLM | 非确定性，仅用于生成文本 |

**原因**：大模型做算术会出错，且同样问题两次询问可能给出不同结果。热量数字的一致性是产品的信任基础，一旦出现前后矛盾，用户信任不可逆地崩塌。

**实施要求**：

- 所有营养计算封装在独立模块 `core/nutrition.ts`，单元测试覆盖
- 传给 LLM 的 Prompt 中，**所有数值由计算层提供**，禁止让 LLM 自行推算热量
- LLM 返回结果若包含数值，前端需做合理性校验（范围校验），异常则回退到计算层数值

### 5.3 云函数清单

| 函数名 | 职责 |
|---|---|
| `recognizeFood` | 接收图片 → 调用视觉模型 → 返回食物列表与热量 |
| `generateAdvice` | 接收当日饮食汇总 + 营养目标 → 返回改进建议 |
| `generateRecommend` | 接收剩余额度 + 场景 + 偏好 → 返回 3 个选项 |
| `generatePlan` | 接收 TDEE / 目标热量 / 营养目标 / 用户偏好 → 返回饮食计划与运动计划 |
| `generateWeeklyReport` | 接收一周数据 → 返回周报 |

### 5.4 性能与体验要求

| 项 | 要求 |
|---|---|
| 拍照识别响应 | ≤ 5 秒，超时提供手动兜底 |
| 首页加载 | 本地缓存当日数据，弱网可用 |
| 分析生成 | 每日首次生成后缓存，避免重复调用 |
| 断网可用 | 记录、查看历史等核心操作支持本地暂存后同步 |

---

## 6. V1 范围

### 6.1 V1 包含

| 模块 | V1 交付内容 |
|---|---|
| 记录 | 拍照识别（可编辑）+ 手动搜索 + 自定义食物 + 餐次分类 + 编辑删除 |
| 分析 | 每日热量汇总 + 三大营养素 + AI 改进建议 |
| 推荐 | 下一顿吃什么（3 选项），支持场景切换 |
| 方案 | 目标设置引导（含极端目标拦截）+ 饮食计划 + 运动计划（周维度） |
| 趋势 | 体重曲线（7 日移动平均）+ 热量趋势 + 打卡日历 |
| 我的 | 档案管理 + 目标管理 + 会员入口 |
| **留存** | **订阅消息召回（餐点提醒 / 补记提醒 / 周报提醒）** |
| **裂变基建** | **战绩卡片模板 + 分享链路 + 内容沉淀的数据结构** |

> **裂变基建必须在 V1 埋好**：分享内容的数据结构、内容沉淀的表设计、分享回流追踪，这些如果后期再补，需要改造既有数据与流程，成本高得多。功能可以 V1.5 再开，基建不能等。

### 6.2 V1.5（上线后 4–8 周）

| 模块 | 内容 | 前置条件 |
|---|---|---|
| 减脂搭子 | 2–5 人小群、进度互见、一键鼓励 | V1 留存指标达标（7 日留存 ≥ 20%） |
| 内容沉淀与检索 | 饮食点评内容库、搜索入口、内容分享 | 内容条目积累 ≥ 1000 条 |
| 战绩卡片上线 | 里程碑触发分享、小程序码回流 | 分享链路埋点验证通过 |

### 6.3 V2 及以后

- 完整的邀请与激励机制
- 食谱详情与烹饪教学
- 条形码扫描
- 智能体脂秤 / 手环设备对接
- 体脂率、身体围度等多维指标
- 多语言 / 深色模式
- 数据导出

**明确不做**：开放社区 / 论坛、好友排行榜、分享解锁功能。

---

## 7. 实施排期

采用**垂直切片**策略：V1 先打通「记录 → 分析 → 推荐 → 方案 → 趋势」完整链路，每层做薄，上线后逐段加深。不做水平铺开（避免每个模块都只做到 60 分）。

| 周次 | 任务 | 交付物 |
|---|---|---|
| W1 | 计算层模块（BMR/TDEE/营养配比/移动平均）+ 单元测试；项目脚手架；目标设置引导页（含拦截逻辑） | `core/nutrition.ts` 及测试；小程序骨架 |
| W2 | 记录模块（拍照识别 + 手动 + 编辑删除）；食物库初始数据（300 条）；首页 | 可用的记录闭环 |
| W3 | AI 改进建议；饮食推荐；趋势页（体重移动平均）；方案页（饮食计划 + 运动计划） | 完整主链路 |
| W4 | 支付接入；会员体系；个体工商户资料提交；提交审核 | 可上线版本 |

---

## 8. 变现设计

主体为个体工商户，已解锁微信支付。

### 8.1 免费 / 付费边界

| 能力 | 免费 | 付费会员 |
|---|---|---|
| 拍照识别 | **无限次** | 无限次 |
| 饮食记录 | 无限 | 无限 |
| 每日热量分析 | ✅ | ✅ |
| AI 改进建议 | 每日 1 条简评 | 不限次 + 完整分析 |
| 饮食推荐 | 每日 1 次 | 不限次 + 场景化方案 |
| **周度饮食计划** | — | ✅ |
| **周度运动计划** | — | ✅ |
| 趋势数据 | 近 7 天 | 全历史 + 多维度对比 |
| 周报 | — | ✅ |

### 8.2 定价建议

| 档位 | 价格 | 说明 |
|---|---|---|
| 月卡 | 9.9 元 | 主推，降低首次付费门槛 |
| 季卡 | 25.9 元 | — |
| 年卡 | 68 元 | 提升 LTV |

首期建议以 9.9 元月卡跑通付费转化，验证后再调整价格。

### 8.3 设计原则

**付费点放在「方案」与「看懂数据」，绝不放「识别次数」。**

按次数收费会让用户不敢记录，与高频留存的目标直接冲突。识别能力应大方免费，价值锚点放在「告诉我接下来怎么办」。

---

## 9. 核心指标

| 阶段 | 指标 | 目标 | 生效版本 |
|---|---|---|---|
| 激活 | 完成目标设置转化率 | ≥ 70% | V1 |
| 留存 | 次日留存 | ≥ 40% | V1 |
| 留存 | 7 日留存 | ≥ 20% | V1 |
| 参与度 | 人均每日记录次数 | ≥ 2.5 | V1 |
| 参与度 | 周活跃用户记录天数 | ≥ 4 天/周 | V1 |
| 付费 | 会员转化率 | ≥ 3% | V1 |
| 质量 | 拍照识别用户修正率 | ≤ 35% | V1 |
| 裂变 | 分享率（分享用户 / 周活跃用户） | ≥ 15% | V1.5 |
| 裂变 | 分享回流转化率 | ≥ 8% | V1.5 |
| 裂变 | K 因子 | ≥ 0.3，V1.5 目标 ≥ 0.6 | V1.5 |
| 社交 | 搭子用户相对无搭子用户 7 日留存提升 | ≥ +10pp | V1.5 |

> **记录频率是最关键的先行指标**。它直接决定数据质量，进而决定 AI 建议的价值，最终决定留存与付费。任何损害记录频率的设计（限次、增加输入成本）都应被否决。

> **裂变的前提是产品本身值得推荐**。记录与分析体验未达标前，裂变只会加速负面口碑扩散——拉来的用户流失更快，且不会再来。因此社交功能集中在 V1.5，V1 只做基建与埋点。

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 视觉识别准确率不足 | 用户不信任，弃用 | 定位为快捷输入，结果可编辑；允许手动兜底；明示估算状态 |
| LLM 生成的数值前后不一致 | 信任崩塌 | 计算层与表达层严格分离（见 5.2） |
| 食物库覆盖不足 | 手动记录体验差 | 优先覆盖高频中餐与外卖菜品；LLM 兜底估算 |
| 视觉模型 API 成本随用户量线性增长 | 毛利被侵蚀 | 识别免费，成本由付费用户摊薄；监控人均调用次数，异常时引入缓存与限流 |
| 用户设置极端目标 | 产品口碑受损，且违背科学性 | 硬性拦截 + 替代方案（见 3.3） |
| 体重日波动导致挫败 | 用户放弃 | 7 日移动平均为主曲线（见 2.5.1） |
| 功能铺开导致周期过长 | 上线延期，反馈滞后 | 垂直切片策略（见第 7 节） |

---

## 附录 A：待确认事项

| # | 事项 | 当前倾向 |
|---|---|---|
| 1 | 首页「下一顿吃什么」的展示强度：轻提示（卡片）还是重展示（直接列 3 个选项） | 重展示，符合「不用动脑」核心命题 |
| 2 | 食物库初期数据获取方式 | AI 生成 + 人工抽检 |
| 3 | 视觉模型与 LLM 供应商选型 | 待定，需对比成本与识别准确率 |
| 4 | 图表库选型：ECharts for 小程序 vs 自绘 Canvas | 视包体积与曲线交互需求决定 |
| 5 | 减脂搭子的匹配维度 | 目标减重量 + 起始体重 + 活跃时段 |
| 6 | 内容沉淀是否对非用户开放搜索 | 待定，需权衡自然流量与用户隐私 |
| 7 | 战绩卡片是否展示真实体重数值 | 建议只展示变化量，不展示绝对值 |
