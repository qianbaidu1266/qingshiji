# 轻食记 · 技术方案

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-29 |
| 配套文档 | 轻食记-PRD-v1.md |
| 前端 | 微信原生小程序 + TypeScript |
| 后端 | 微信云开发（云函数 + 云数据库 + 云存储） |
| 模型服务 | DeepSeek 开放平台（V4 系列） |

> 价格与模型状态信息截至 2026-08-29 检索，DeepSeek 调价频繁，**实施前须以官方定价页与模型列表为准**。

---

## 1. 技术选型总览

| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | 微信原生小程序 + TypeScript | 包体积最小，相机 / Canvas / 订阅消息等能力调用最直接 |
| 服务端 | 微信云开发云函数（Node.js） | 免运维、免备案，天然集成微信鉴权 |
| 数据库 | 云开发数据库（文档型） | 见 PRD 第 4 章数据模型 |
| 文件存储 | 云开发存储 | 存放用户拍摄的餐食图片 |
| 模型服务 | DeepSeek 开放平台 | OpenAI 兼容接口，`base_url = https://api.deepseek.com` |
| 定时任务 | 云开发定时触发器 | 用于错峰批量生成（见第 7 章） |

---

## 2. 模型选型

### 2.1 DeepSeek 当前模型状态（2026-08-29）

| 模型 ID | 类型 | 状态 | 图片输入 |
|---|---|---|---|
| `deepseek-v4-flash` | 文本 | 正式版 | ❌ 不支持 |
| `deepseek-v4-pro` | 文本 | 正式版 | ❌ 不支持 |
| `deepseek-v4-flash-vision-exp` | 多模态 | **实验性（Exp）** | ✅ 支持 |
| ~~`deepseek-chat`~~ | 文本 | **已停服（2026-07-24）** | ❌ |
| ~~`deepseek-reasoner`~~ | 文本 | **已停服（2026-07-24）** | ❌ |

**关键提醒**：网上大量教程仍在使用 `deepseek-chat` / `deepseek-reasoner`，这两个 ID 已停止服务。新项目必须使用 V4 系列 ID。

### 2.2 本项目的模型分配

| 功能 | 模型 | 说明 |
|---|---|---|
| 拍照识别 | `deepseek-v4-flash-vision-exp` | 唯一支持图片输入的 DeepSeek 模型 |
| 每日 AI 改进建议 | `deepseek-v4-flash` | 任务简单，Flash 足够 |
| 饮食推荐 | `deepseek-v4-flash` | 同上 |
| 减脂方案生成（饮食 + 运动） | `deepseek-v4-pro` | 推理链较长，需要更好的编排能力 |
| 周报生成 | `deepseek-v4-pro` | 同上 |
| 食物数据兜底估算 | `deepseek-v4-flash` | 库内查不到时的补充 |

### 2.3 风险：视觉模型为实验性质

`deepseek-v4-flash-vision-exp` 带 `exp` 后缀，属实验性模型，存在以下风险：

- 可能在无预告情况下调整或下线
- 稳定性与响应时延不承诺 SLA
- 识别准确率未经充分验证

**对策（必须实施）**：

1. **抽象识别服务层**。业务代码不直接调用 DeepSeek SDK，而是调用内部统一的 `visionService.recognize(imageUrl)`，便于替换供应商。
2. **备选方案预研**，按优先级：
   - 智谱 GLM-4.6V（见 2.4）
   - 阿里通义千问 Qwen3-VL 系列
   - 腾讯混元视觉（腾讯生态，网络链路可能更优）
   - 字节豆包视觉理解
3. **识别结果可编辑**是产品层面的兜底（PRD 2.1.1），即使模型波动也不阻塞用户。

### 2.4 备选供应商：智谱 GLM

| 项 | 说明 |
|---|---|
| 开放平台 | https://open.bigmodel.cn |
| API 地址 | `https://open.bigmodel.cn/api/paas/v4` |
| 协议 | OpenAI 兼容，可与 DeepSeek 复用同一套 SDK |

**视觉模型价格对比（人民币 / 百万 tokens，2026-08 检索，须以官方定价页为准）**

| 模型 | 输入 | 输出 | 备注 |
|---|---|---|---|
| DeepSeek V4-Flash（高峰） | 3.0 | 9.0 | 工作日 9–12、14–18 点 |
| DeepSeek V4-Flash（空闲） | 1.5 | 4.5 | 其余时段与周末 |
| 智谱 GLM-4.6V | 约 2.03 | 约 6.1 | **不分峰谷** |
| 智谱 GLM-4.5V | 约 4.07 | 约 12.2 | 上一代视觉模型 |
| 智谱 GLM-5.3-Flash | 约 0.51 | 约 1.7 | **支持视觉，价格极低** |

**三点观察**：

1. **GLM-4.6V 相比 DeepSeek 高峰价便宜约 1/3，且无峰谷限制**——意味着若改用智谱，架构上可不作为错峰做复杂调度，设计更简单。
2. **GLM-5.3-Flash 价格极低**（输出仅为 DeepSeek 空闲价的 38%），若识别准确率达标，视觉成本可再降 60% 以上。
3. 不同数据来源对智谱报价存在出入，**选型前必须以官方定价页与实测为准**。

**建议的选型路径**：

| 阶段 | 方案 |
|---|---|
| 起步 | DeepSeek V4-Flash / V4-Pro 承担全部文本任务；视觉任务实测 DeepSeek Vision 与 GLM-4.6V |
| 验证后 | 按「实测准确率 × 价格」择优固定视觉供应商 |
| 降本阶段 | 测试 GLM-5.3-Flash 的视觉准确率，达标则切换 |

> 无论选哪家，`visionService` 抽象层必须保留。模型供应商在 12 个月内发生变更是大概率事件。

---

## 3. 部署与服务架构

### 3.1 数据存哪里、后端在哪里

**一句话：全部在微信云开发上，不需要买服务器，不需要备案。**

```
┌────────────────────────────────────────────┐
│  微信小程序（前端 · TypeScript）              │
│                                             │
│   ┌───────────────┐   ┌─────────────────┐  │
│   │ 计算层（本地）  │   │ 本地缓存         │  │
│   │ BMR / TDEE    │   │ 今日记录、食物库 │  │
│   │ 目标热量       │   │                 │  │
│   │ 营养配比       │   │                 │  │
│   │ 7日移动平均    │   │                 │  │
│   └───────────────┘   └─────────────────┘  │
└──────┬────────────┬─────────────┬──────────┘
       │            │             │
       │ ①直连读写   │ ②调用云函数  │ ③上传图片
       │            │             │
┌──────▼────────────▼─────────────▼──────────┐
│        微信云开发 CloudBase（腾讯云）          │
│                                             │
│  ┌──────────────┐  ┌──────────────────────┐ │
│  │ 云数据库      │  │ 云函数（Node.js）     │ │
│  │ ← 业务数据    │  │ ← 后端服务在这里      │ │
│  │   7 个集合    │  │   5 个函数           │ │
│  └──────────────┘  └──────────┬───────────┘ │
│  ┌──────────────┐  ┌──────────┴───────────┐ │
│  │ 云存储        │  │ 定时触发器            │ │
│  │ ← 餐食图片    │  │ ← 错峰批量生成        │ │
│  └──────────────┘  └──────────────────────┘ │
│  ┌──────────────────────────────────────┐   │
│  │ 云调用：免鉴权发送订阅消息              │   │
│  └──────────────────────────────────────┘   │
└──────────────────┬──────────────────────────┘
                   │ ④ HTTPS
        ┌──────────▼───────────┐
        │  DeepSeek / 智谱 API  │
        │  （经云函数中转）      │
        └──────────────────────┘
```

### 3.2 微信云开发是什么

微信云开发是微信官方提供的 **Serverless 后端服务**，在微信开发者工具里直接开通即可使用。

| 能力 | 作用 | 本项目用在哪 |
|---|---|---|
| **云数据库** | 文档型数据库（类似 MongoDB），数据以 JSON 文档存储 | 存用户档案、饮食记录、体重、方案等全部业务数据 |
| **云函数** | Node.js 运行环境，写后端逻辑 | AI 调用、数值校验、聚合统计、定时任务 |
| **云存储** | 文件存储 | 存用户拍摄的餐食图片 |
| **云调用** | 云函数中免鉴权调用微信开放能力 | 发送订阅消息（不需要自己处理 access_token） |
| **定时触发器** | Cron 定时任务 | 凌晨错峰批量生成点评与方案 |
| **静态托管** | 托管网页 | 用于隐私政策、用户协议页面 |

**最省事的三点**：

1. **免服务器、免域名、免备案**——传统后端要买云服务器、买域名、做 ICP 备案（通常要 1-3 周），云开发全都不用
2. **天然鉴权**——云函数里一行 `cloud.getWXContext().OPENID` 直接拿到用户身份，不用自己做登录体系
3. **与微信生态深度集成**——发订阅消息、获取用户信息、内容安全检测都是免鉴权直调

### 3.3 为什么不用自建后端

| 对比项 | 微信云开发 | 自建（Spring Boot + 云服务器） |
|---|---|---|
| 服务器 | 无需 | 需购买 + 运维 |
| 域名与备案 | 无需 | 需域名 + ICP 备案（1-3 周） |
| 登录鉴权 | 内置 openid | 需自建微信登录流程 |
| 微信能力调用 | 云调用免鉴权 | 需自行维护 access_token |
| 运维成本 | 接近零 | 持续投入 |
| 费用（早期） | 免费额度内基本为零 | 服务器最低也要几十元/月起 |
| 适用阶段 | 适合 0→1 验证 | 适合规模化和复杂业务 |

> 用户主业技术栈为 Java / Spring Boot，但本项目**不建议自建后端**。小程序 0→1 阶段，云开发节省的备案时间与运维精力，远大于技术栈熟悉度带来的收益。待业务规模扩大、出现复杂业务规则时再考虑迁移。

### 3.4 环境划分

| 环境 | 用途 | 说明 |
|---|---|---|
| 开发环境 `dev` | 日常开发与联调 | 独立数据库，测试数据随意造 |
| 生产环境 `prod` | 正式发布 | 通过云开发控制台做数据隔离 |

两个环境使用独立的环境 ID，在 `app.js` 初始化时通过变量切换，**禁止硬编码环境 ID**。

### 3.5 数据读写策略

云开发允许前端 SDK 直连数据库（受安全规则保护）。本项目采用混合策略：

| 操作类型 | 方式 | 理由 |
|---|---|---|
| 查询今日记录、历史记录 | **前端直连** | 更快、不消耗云函数资源 |
| 写入 / 修改 / 删除记录 | **走云函数** | 需要字段校验与业务规则 |
| 所有 AI 相关调用 | **走云函数** | 必须，API Key 不能落前端 |
| 统计聚合（周报、趋势） | **走云函数** | 聚合运算在服务端更高效 |
| 食物库查询 | **前端直连** | 只读公共数据 |

### 3.6 数据存储设计（数据库）

数据库为文档型，共 7 个集合。字段定义见 PRD 第 4 章，此处补充索引、权限与示例。

#### 集合总览与权限设置

> **权限设置是云开发的关键安全机制**，控制前端直连时的数据可见范围。

| 集合 | 说明 | 权限设置 | 理由 |
|---|---|---|---|
| `users` | 用户档案 | 仅创建者可读写 | 个人隐私 |
| `goals` | 减脂目标 | 仅创建者可读写 | 个人隐私 |
| `foodLogs` | 饮食记录 | 仅创建者可读写 | 个人隐私 |
| `weightLogs` | 体重记录 | 仅创建者可读写 | 个人隐私（敏感） |
| `plans` | 减脂方案 | 仅创建者可读写 | 个人隐私 |
| `dailyAnalysis` | 每日分析缓存 | 仅创建者可读写 | 个人隐私 |
| `foodLibrary` | 食物库 | **所有用户可读，仅管理端可写** | 公共数据，不允许用户篡改 |

#### 索引设计

| 集合 | 索引 | 类型 | 用途 |
|---|---|---|---|
| `foodLogs` | `(_openid, date)` | 复合，非唯一 | 按日期查当日全部记录（最高频查询） |
| `foodLogs` | `(_openid, date, mealType)` | 复合，非唯一 | 按餐次分组查询 |
| `weightLogs` | `(_openid, date)` | 复合，**唯一** | 每人每天仅一条体重记录 |
| `dailyAnalysis` | `(_openid, date)` | 复合，**唯一** | 每日分析唯一，便于缓存命中判断 |
| `goals` | `(_openid, status)` | 复合，非唯一 | 查询当前生效目标 |
| `plans` | `(_openid, type, startDate)` | 复合，非唯一 | 查询当前方案 |
| `foodLibrary` | `name` | 单字段 | 搜索匹配 |
| `foodLibrary` | `aliases` | 单字段，数组 | 别名搜索 |

> 云开发数据库需**手动创建索引**（控制台或 `db.collection().createIndex()`）。缺少索引时，数据量上万后查询性能会显著下降，且可能触发默认 limit 限制。

#### 示例文档

**foodLogs（饮食记录）**

```json
{
  "_id": "a1b2c3d4",
  "_openid": "oXxx_xxxxxxxx",
  "date": "2026-08-29",
  "mealType": "lunch",
  "foodName": "香煎鸡胸沙拉",
  "portion": 200,
  "portionLabel": "1 份",
  "calorie": 386,
  "protein": 38,
  "fat": 16,
  "carb": 22,
  "source": "photo",
  "isEstimated": false,
  "imageUrl": "cloud://prod-xxx/meals/oXxx/20260829/1701234567.jpg",
  "loggedAt": "2026-08-29T04:12:33.000Z"
}
```

**goals（目标）**

```json
{
  "_id": "g1h2i3j4",
  "_openid": "oXxx_xxxxxxxx",
  "startWeight": 60.0,
  "currentWeight": 58.6,
  "targetWeight": 55.0,
  "weeklyRate": 0.6,
  "bmr": 1319,
  "tdee": 1583,
  "calorieTarget": 1250,
  "proteinTarget": 108,
  "fatTarget": 54,
  "carbTarget": 108,
  "status": "active",
  "createdAt": "2026-08-01T02:00:00.000Z"
}
```

> 注意：`bmr` / `tdee` / `calorieTarget` 等计算结果为**冗余存储**。目的是保留生成方案时的历史数值，便于追溯与审计，即使后续算法调整，历史方案仍可解释。

**foodLibrary（食物库）**

```json
{
  "_id": "f1g2h3i4",
  "name": "番茄炒蛋",
  "aliases": ["西红柿炒鸡蛋", "番茄炒鸡蛋"],
  "category": "家常菜",
  "caloriePer100g": 90,
  "proteinPer100g": 6,
  "fatPer100g": 6,
  "carbPer100g": 4,
  "commonPortions": [
    { "label": "1 份（约 200g）", "grams": 200 },
    { "label": "1 盘（约 350g）", "grams": 350 }
  ]
}
```

### 3.7 云存储设计

| 项 | 设计 |
|---|---|
| 目录结构 | `meals/{openid}/{yyyyMMdd}/{timestamp}.jpg` |
| 权限 | 仅创建者可读写 |
| 图片处理 | **上传前前端压缩至长边 ≤ 1080px**，质量 0.8 |
| 生命周期 | 建议设置 90 天后转低频存储，节省成本 |

**为什么要压缩**：原图通常 3-5MB，压缩后约 200-400KB。这直接影响三件事——上传速度（用户体验）、存储成本、以及后续传给视觉模型的传输耗时。而识别准确率在 1080px 下基本无损（模型内部也会缩放到约 800×800）。

### 3.8 架构原则：计算层与表达层严格分离

（承接 PRD 5.2，此处为实施要求）

- **计算层**：BMR、TDEE、目标热量、热量缺口、营养素配比、7 日移动平均，全部由纯 TypeScript 函数在**前端本地**执行。确定性输出，无网络往返。
- **表达层**：云函数调用 LLM，仅负责把算好的数值翻译为自然语言方案。

**实施约束**：

```
所有营养计算封装于 core/nutrition.ts，需单元测试覆盖。
传给模型的 Prompt 中，所有数值由计算层提供。
Prompt 中必须显式声明「不要自行计算，使用提供的数值」。
模型返回的数值需做范围校验，异常时回退到计算层数值。
```

---

## 4. 云函数设计

### 4.1 云函数清单

| 函数名 | 模型 | 触发方式 | 超时配置 |
|---|---|---|---|
| `recognizeFood` | vision-exp | 用户拍照后主动调用 | 60s（识别较慢） |
| `generateAdvice` | v4-flash | 打开首页 / 定时任务预生成 | 20s |
| `generateRecommend` | v4-flash | 用户点击推荐位 | 20s |
| `generatePlan` | v4-pro | 用户创建目标 / 定时任务 | 60s |
| `generateWeeklyReport` | v4-pro | 定时任务（空闲时段） | 60s |

> 微信云函数默认超时 20 秒，可在配置中调整至最长 60 秒。视觉识别与方案生成链路较长，须显式配置。

### 4.2 云函数骨架

```javascript
// cloudfunctions/recognizeFood/index.js
const cloud = require('wx-server-sdk')
const axios = require('axios')

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

const DEEPSEEK_URL = 'https://api.deepseek.com/chat/completions'
const MODEL = 'deepseek-v4-flash-vision-exp'

// API Key 从环境变量读取，禁止硬编码
const API_KEY = process.env.DEEPSEEK_API_KEY

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { imageUrl } = event

  // 1. 参数校验
  if (!imageUrl) return { code: 400, msg: '缺少图片' }

  // 2. 频率限制（防止刷量，按 openid 限流）
  const allowed = await checkRateLimit(OPENID)
  if (!allowed) return { code: 429, msg: '请求过于频繁' }

  try {
    // 3. 调用模型
    const res = await axios.post(
      DEEPSEEK_URL,
      {
        model: MODEL,
        messages: [
          { role: 'system', content: SYSTEM_PROMPT },
          {
            role: 'user',
            content: [
              { type: 'text', text: USER_PROMPT },
              { type: 'image_url', image_url: { url: imageUrl } }
            ]
          }
        ],
        response_format: { type: 'json_object' },
        temperature: 0.3,
        max_tokens: 1200
      },
      {
        headers: { Authorization: `Bearer ${API_KEY}` },
        timeout: 45000
      }
    )

    // 4. 解析与校验
    const raw = res.data.choices[0].message.content
    const parsed = JSON.parse(raw)
    const validated = validateFoodItems(parsed)  // 数值范围校验

    return { code: 0, data: validated }
  } catch (err) {
    // 5. 失败兜底，绝不让流程中断
    console.error('[recognizeFood]', err.message)
    return { code: 500, msg: '识别失败，请手动添加', fallback: 'manual' }
  }
}
```

### 4.3 数值校验函数

```typescript
// core/validate.ts
// 模型返回的数值必须落在合理区间，超出则标记为估算或拒绝
const RANGES = {
  caloriePer100g: [0, 900],   // 油脂类上限
  proteinPer100g: [0, 100],
  fatPer100g:     [0, 100],
  carbPer100g:    [0, 100],
}

export function validateFoodItems(parsed) {
  return parsed.items.map(item => {
    const flag = (v, [min, max]) => v >= min && v <= max
    const ok =
      flag(item.calorie, RANGES.caloriePer100g) &&
      flag(item.protein, RANGES.proteinPer100g) &&
      flag(item.fat,     RANGES.fatPer100g) &&
      flag(item.carb,    RANGES.carbPer100g)

    return {
      ...item,
      isEstimated: !ok || item.confidence < 0.6,
      // 三大营养素热量与总热量偏差超过 20% 时，以总热量为准标记估算
      nutritionConsistent:
        Math.abs(item.protein * 4 + item.fat * 9 + item.carb * 4 - item.calorie)
        / item.calorie < 0.2
    }
  })
}
```

---

## 5. Prompt 工程

### 5.1 通用要点

| 要点 | 做法 |
|---|---|
| 结构化输出 | 开启 `response_format: { type: 'json_object' }`，Prompt 中须出现 "JSON" 字样 |
| 温度 | 识别与方案生成用 `0.3`（稳定）；点评与推荐用 `0.7`（有趣） |
| 数值禁算 | Prompt 中显式声明「所有数值使用提供的数据，不要自行计算」 |
| 长度控制 | 用 `max_tokens` 限制，防止输出失控 |
| 中文语境 | 明确指定中餐场景（外卖份量、常见菜品、中式烹饪方式） |

### 5.2 拍照识别 Prompt

```
你是营养分析助手。识别图片中的食物，输出 JSON 格式结果。

【背景】
用户为中国年轻上班族，常见就餐场景为外卖、食堂、便利店。
份量请按中国大陆常见份量估算。

【要求】
1. 只输出 JSON，不要任何解释性文字
2. 每种食物给出：名称、份量(g)、热量(kcal)、蛋白质(g)、脂肪(g)、碳水(g)、置信度(0-1)
3. 份量取常见值，不要精确到个位
4. 外卖份量通常比家常份量大 15% 左右，按此修正
5. 汤汁、酱料热量需单独估算并计入
6. 无法确定的食物也要给出估算，置信度标为 0.6 以下

【输出格式】
{
  "items": [
    {"name": "番茄炒蛋", "portion": 200, "calorie": 180,
     "protein": 12, "fat": 11, "carb": 6, "confidence": 0.85}
  ],
  "total": {"calorie": 180, "protein": 12, "fat": 11, "carb": 6},
  "note": "一句话提示，如：汤汁热量未计入"
}
```

### 5.3 每日改进建议 Prompt

```
你是减脂营养顾问。基于以下数据生成今日饮食点评。

【用户档案】女，28岁，160cm，60kg，轻活动水平
【今日目标】热量 1250 kcal，蛋白质 108g，脂肪 54g，碳水 108g
【实际摄入】热量 1420 kcal，蛋白质 77g，脂肪 62g，碳水 130g
【饮食明细】
- 早餐：燕麦牛奶碗 268 kcal
- 午餐：红烧牛肉面 542 kcal
- 加餐：奶茶 420 kcal
- 晚餐：番茄豆腐汤 190 kcal

【要求】
1. 3-5 句话，口语化，允许幽默调侃，禁止人身攻击
2. 给出 2-3 条具体到食物的改进建议（如「晚餐加两个水煮蛋」）
3. 禁止解释营养学概念（不要说「蛋白质很重要」）
4. **所有数值使用上方提供的数据，不要自行计算或推算**
5. 输出 JSON，只输出 JSON

【输出格式】
{
  "summary": "点评文字",
  "suggestions": ["建议1", "建议2"]
}
```

### 5.4 减脂方案生成 Prompt

```
你是减脂方案设计师。基于以下已计算完成的数值，生成一周饮食计划与运动计划。

【用户档案】女，28岁，160cm，60kg，久坐（办公室）
【已计算数值 - 直接使用，不要重新计算】
  BMR = 1319 kcal
  TDEE = 1583 kcal
  每日目标热量 = 1250 kcal
  每日蛋白质目标 = 108g
  每日脂肪目标 = 54g
  每日碳水目标 = 108g
  每周目标减重 = 0.6kg
【偏好】忌口：无；可运动时段：工作日 20:00 后；运动基础：几乎为零

【要求】
1. 饮食计划：给出 7 天，每天早/午/晚三餐的具体食物组合与热量分配
   - 三餐热量之和必须约等于 1250 kcal
   - 每餐标注该餐热量区间
   - 食物需符合上班族场景（外卖、食堂、便利店可获取），不要给需要复杂烹饪的食谱
2. 运动计划：给出 7 天，每天的运动类型、时长、强度、执行要点
   - 用户几乎无运动基础，前两周强度必须低
   - 每周至少安排 1 天休息
   - 消耗给范围值（如「约 200-250 大卡」），不要给精确数字
3. **禁止自行计算热量数值，使用上方提供的数字**
4. 输出 JSON，只输出 JSON

【输出格式】
{
  "dietPlan": [
    {"day": 1, "meals": [
      {"type": "breakfast", "items": ["食物组合"], "calorieRange": [300, 350], "tips": "要点"}
    ]}
  ],
  "exercisePlan": [
    {"day": 1, "type": "快走", "duration": "30分钟",
     "intensity": "低", "calorieRange": [120, 160], "tips": "要点"}
  ]
}
```

---

## 6. 数值一致性保障

（对应 PRD 5.2，此处为具体措施）

| 措施 | 说明 |
|---|---|
| 计算前置 | BMR/TDEE/目标热量/营养配比在前端算完，作为参数传入云函数 |
| Prompt 显式约束 | 所有 Prompt 均包含「不要自行计算，使用提供的数值」 |
| 返回校验 | 云函数与前端双层校验：范围校验 + 三大营养素热量与总热量一致性校验（偏差 < 20%） |
| 异常回退 | 校验失败时以计算层数值为准，并标记 `isEstimated` |
| 缓存 | 每日点评、方案生成后缓存，避免重复调用产生不同结果 |

---

## 7. 成本测算与优化

### 7.1 DeepSeek 现行价格（2026-08-17 生效，峰谷定价）

高峰时段：北京时间 9:00–12:00、14:00–18:00（工作日）
空闲时段：其余时间，及周末全天

| 模型 | 时段 | 缓存命中输入 | 缓存未命中输入 | 输出 |
|---|---|---|---|---|
| V4-Flash | 空闲 | 0.05 | 1.5 | 4.5 |
| V4-Flash | 高峰 | 0.10 | 3.0 | 9.0 |
| V4-Pro | 空闲 | 0.15 | 4.5 | 13.5 |
| V4-Pro | 高峰 | 0.30 | 9.0 | 27.0 |

单位：元 / 百万 tokens。图片按尺寸折算 token，单图上限 384 tokens。

> 注意：较 8 月 17 日前（Flash 输入 1 元 / 输出 2 元）**显著上涨**，高峰时段输出涨幅达 350%。成本模型须按现行价格建立。

### 7.2 成本测算（1000 日活，高峰价格）

| 功能 | 输入 tokens | 输出 tokens | 单次的成本 | 频次 | 月成本 |
|---|---|---|---|---|---|
| 每日点评 | 800 | 300 | 0.0051 元 | 1 次/天 | 153 元 |
| 拍照识别 | 784（含图 384） | 300 | 0.0050 元 | 2.5 次/天 | 375 元 |
| 饮食推荐 | 600 | 300 | 0.0045 元 | 2 次/天 | 270 元 |
| 方案生成 | 1500 | 2000 | 0.0225 元 | 1 次/周 | 96 元 |
| **合计** | | | | | **约 894 元 / 月** |

### 7.3 优化措施

**措施一：错峰调用（收益最大，成本可降约 40%）**

| 功能 | 实时性要求 | 是否可错峰 | 策略 |
|---|---|---|---|
| 拍照识别 | 高，用户等待中 | ❌ 不可 | 保持实时 |
| 每日点评 | 低 | ✅ 可 | 凌晨定时任务批量预生成，用户打开即读缓存 |
| 饮食推荐 | 中 | ✅ 可 | 按餐次提前生成并缓存 |
| 方案生成 | 低 | ✅ 可 | 定时任务，空闲时段生成 |
| 周报 | 低 | ✅ 可 | 周日空闲时段批量生成 |

**措施二：提高缓存命中率**

固定且较长的 system prompt 前置，可命中缓存（输入成本从 3 元降至 0.1 元/百万，降幅 96%）。要求：system prompt 内容稳定，变量数据放在 user message。

**措施三：控制调用频次**

- 每日点评缓存到 `dailyAnalysis` 表，仅当新增记录时才重新生成
- 推荐结果按「餐次 + 日期」缓存
- 方案按周缓存，不重复生成

**优化后预估**：综合错峰 + 缓存，月成本可降至 **400–500 元 / 1000 日活**。

### 7.4 商业提醒

按 PRD 变现模型测算：1000 日活 × 3% 转化 × 9.9 元 = **297 元/月会员收入**，**低于模型成本**。

因此：

- 成本优化不是可选项，是必选项
- 需叠加流量主广告收入（PRD 未计入）
- 需关注人均调用次数，异常用户需限流
- 建议设置调用预算告警

### 7.5 若改用智谱的经济性

若视觉任务改用 GLM-4.6V（输出 ¥6.1/百万，不分峰谷）：

| 方案 | 拍照识别月成本（1000 日活） | 综合月成本 |
|---|---|---|
| DeepSeek 全高峰 | 375 元 | 约 894 元 |
| DeepSeek 全空闲（理想错峰） | 188 元 | 约 707 元 |
| **DeepSeek 文本 + GLM-4.6V 视觉** | 约 260 元 | 约 780 元 |
| 进一步切 GLM-5.3-Flash（若达标） | 约 90 元 | 约 610 元 |

**智谱方案的核心价值不是绝对低价，而是"不依赖错峰调度"**。省去定时任务、预生成、缓存失效判断这一整套复杂度，对 0→1 阶段是实质性减负。若实测准确率相当，**建议优先智谱**。

### 7.6 云开发自身成本

模型成本之外，微信云开发自身也有费用（有免费额度，超出按量计费）：

| 计费项 | 消耗估算（1000 日活） | 关注点 |
|---|---|---|
| 数据库读写次数 | 约 60 万次 / 月 | 索引缺失会放大读写次数 |
| 云函数资源使用量 | 约 10 万次调用 / 月 | AI 函数耗时长，GBs 消耗较大 |
| 云存储容量 | **约 22 GB / 月增长** | ⚠️ 持续累积，需生命周期管理 |
| CDN 回源流量 | 视图片访问量 | 图片直传不经过 CDN 可省 |

> ⚠️ **存储是隐性成本大头**：按每人每天 2.5 张、压缩后 300KB 计算，月增约 22GB 且只增不减。必须在上线前配置存储生命周期（90 天后转低频或删除原图，仅保留识别结果与缩略图），否则长期成本会失控。

**建议**：上线前在云开发控制台配置费用告警阈值，并按周查看用量报表。

---

## 8. 失败兜底

| 故障类型 | 处理 |
|---|---|
| 模型超时 | 云函数 timeout 45s，超时返回兜底提示 + 手动入口 |
| 模型返回格式错误 | JSON 解析失败时重试 1 次，仍失败则返回手动入口 |
| 模型限流 | 云函数层排队或提示稍后重试 |
| 网络异常 | 前端本地暂存记录，恢复后同步 |
| 识别结果明显异常 | 数值校验标记 `isEstimated`，UI 明确提示，用户可修正 |

**原则**：任何 AI 环节失败，都不能阻塞用户完成记录。记录是核心行为，AI 是增强。

---

## 9. 安全

| 项 | 措施 |
|---|---|
| API Key | 存于云函数环境变量，禁止硬编码、禁止下发前端。小程序前端可被反编译，Key 落前端等于公开 |
| 接口鉴权 | 云函数通过 `cloud.getWXContext()` 获取 openid，校验数据归属 |
| 频率限制 | 按 openid 限制调用频次，防止刷量 |
| 用户数据 | 体重、饮食属个人敏感信息，须有隐私政策并明示用途 |
| 图片存储 | 云存储设置访问权限，仅本人及云函数可读 |

---

## 10. 开发清单

| 状态 | 优先级 | 任务 | 说明 |
|---|---|---|---|
| ✅ 已完成 | P0 | `core/nutrition.ts` 计算层 + 单元测试 | **32 个测试全部通过**，`npm test` 验证。含 BMR/TDEE、目标热量、营养配比、7 日移动平均、极端目标拦截 |
| ⬜ 待办 | P0 | **开通云开发，创建 dev / prod 两套环境** | 见 3.4，需先有 AppID，环境 ID 不硬编码 |
| ⬜ 待办 | P0 | **创建 7 个数据库集合并配置权限** | 见 3.6，权限设置错误会导致数据泄露 |
| ⬜ 待办 | P0 | **创建数据库索引** | 见 3.6，缺索引会导致查询变慢与 limit 限制 |
| ⬜ 待办 | P0 | 云函数基础框架 | 统一鉴权、限流、错误处理、日志 |
| ⬜ 待办 | P0 | `recognizeFood` + Prompt 调优 | 含识别服务抽象层，便于换供应商 |
| ⬜ 待办 | P0 | 数值校验模块 | 范围校验 + 一致性校验 |
| ⬜ 待办 | P0 | 前端图片压缩（长边 ≤ 1080px） | 影响上传速度、存储成本与识别耗时 |
| ⬜ 待办 | P1 | 食物库初始数据（300 条） | AI 生成 + 人工抽检 |
| ⬜ 待办 | P1 | `generateAdvice` / `generateRecommend` | 含缓存机制 |
| ⬜ 待办 | P1 | `generatePlan`（饮食 + 运动） | 文本任务用 v4-pro |
| ⬜ 待办 | P1 | **配置云存储生命周期** | 见 7.6，存储是隐性成本大头 |
| ⬜ 待办 | P1 | 订阅消息接入（云调用） | 留存第一手段，见 PRD 2.7.5 |
| ⬜ 待办 | P2 | 定时任务与错峰调度 | 若使用 DeepSeek 则关键；改用智谱可简化 |
| ⬜ 待办 | P2 | 成本监控与告警 | 按用户、按功能统计 token 消耗 |

---

## 附录：待验证事项

| # | 事项 | 验证方式 |
|---|---|---|
| 1 | `deepseek-v4-flash-vision-exp` 对中餐的识别准确率 | 准备 50 张常见中餐/外卖图片实测 |
| 2 | 视觉模型响应时延 | 实测 P50 / P95，确认是否需要压缩图片 |
| 3 | JSON Output 模式稳定性 | 连续调用 100 次，统计格式错误率 |
| 4 | 图片压缩策略 | 压缩后识别准确率是否可接受（直接影响成本与延迟） |
| 5 | 备选供应商对比 | 同测试集对比 Qwen-VL / GLM-4V 的准确率与价格 |
